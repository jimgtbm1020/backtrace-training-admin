'use client';
import {useEffect,useState} from 'react';
import {createClient} from '@supabase/supabase-js';
const sb=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);
export default function EmailSettings(){const [data,setData]=useState<any>(null),[error,setError]=useState(''),[loading,setLoading]=useState(true);
 useEffect(()=>{(async()=>{const {data:session}=await sb.auth.getSession();if(!session.session){setError('Administrator sign-in is required.');setLoading(false);return}const {data:dashboard,error}=await sb.rpc('get_training_email_delivery_dashboard');if(error)setError(error.message);else setData(dashboard);setLoading(false)})()},[]);
 if(loading)return <main className="shell"><section className="card"><h1>Email Settings</h1><p>Loading communications configuration…</p></section></main>;
 return <main className="shell legacy-admin-page"><header className="legacy-page-header"><div><h1>Email Settings</h1><p>Review communications configuration and delivery safeguards.</p></div><a href="/">Back to dashboard</a></header>{error?<section className="card"><p className="error">{error}</p></section>:<><section className="settings-status-grid"><div><span>DELIVERY</span><strong>OFF</strong><small>Manual activation required</small></div><div><span>QUEUE PENDING</span><strong>{data?.queue?.pending||0}</strong><small>No automatic delivery</small></div><div><span>DELIVERED</span><strong>{data?.queue?.delivered||0}</strong><small>Provider events</small></div><div><span>FAILED</span><strong>{(data?.queue?.failed||0)+(data?.queue?.provider_failed||0)}</strong><small>Requires review</small></div></section><section className="legacy-panel"><h2>Provider Settings</h2><div className="settings-grid"><div><label>Provider</label><strong>Resend</strong></div><div><label>Sender</label><strong>Backtrace Training</strong></div><div><label>From Address</label><strong>no-reply@training.gtbm.com</strong></div><div><label>Automatic Delivery</label><strong>Disabled</strong></div><div><label>Tracking</label><strong>{data?.webhook?.enabled?'Enabled':'Disabled'}</strong></div><div><label>Webhook Signature</label><strong>{data?.webhook?.configured?'Configured':'Not configured'}</strong></div></div></section><section className="legacy-panel"><div className="panel-heading"><h2>Email Templates</h2><span>{(data?.templates||[]).length} templates</span></div><div className="simple-list">{(data?.templates||[]).map((row:any)=><div key={row.alias||row.display_name}><strong>{row.display_name||row.alias}</strong><span>{row.active===false?'Inactive':row.status||'Configured'}</span></div>)}</div></section><section className="legacy-panel"><div className="panel-heading"><h2>Recent Delivery Events</h2><span>{(data?.recent_events||[]).length} events</span></div>{data?.recent_events?.length?<div className="simple-list">{data.recent_events.slice(0,10).map((row:any,i:number)=><div key={row.queue_id||i}><strong>{row.event_type||'Event'}</strong><span>{row.recipient_email||'No recipient'} · {row.event_at?new Date(row.event_at).toLocaleString():''}</span></div>)}</div>:<p className="muted">No delivery events recorded.</p>}</section></>}</main>}