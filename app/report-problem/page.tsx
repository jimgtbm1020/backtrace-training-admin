'use client';
import {useState} from 'react';
export default function ReportProblem(){const [type,setType]=useState('Application problem');const [description,setDescription]=useState('');const [sent,setSent]=useState(false);function submit(){const subject=encodeURIComponent(type+' - Training Administration');const body=encodeURIComponent(description);window.location.href='mailto:support@backtracereports.com?subject='+subject+'&body='+body;setSent(true)}return <main className="shell"><section className="card report-card"><h1>Report a Problem</h1><p className="subtitle">Tell us what happened so it can be reviewed.</p><label>Problem type<select value={type} onChange={e=>setType(e.target.value)}><option>Application problem</option><option>Data problem</option><option>Access problem</option><option>Other</option></select></label><label>Description<textarea value={description} onChange={e=>setDescription(e.target.value)} placeholder="Describe the problem..." rows={7}/></label><button disabled={!description.trim()} onClick={submit}>Open Email Report</button>{sent&&<p className="subtitle">Your email application should now be open.</p>}<a href="/">Back to dashboard</a></section></main>}